@page
@model WebApp.Pages.GeneratingStationClassifications.IndexModel
@{
    ViewData["Title"] = "GeneratingStation Classifications";
}

<h1>GeneratingStation Classifications</h1>

<p>
    <a asp-page="./Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.GeneratingStationClassifications)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Classification)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

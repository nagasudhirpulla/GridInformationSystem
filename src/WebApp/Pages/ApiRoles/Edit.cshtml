@page
@model WebApp.Pages.ApiRoles.EditModel
@{
    ViewData["Title"] = "Edit ApiRole";
}

<div class="row justify-content-center align-items-center">
    <div class="col col-md-4 col-lg-4 col-xl-3">
        <h2>@ViewData["Title"]</h2>
        <form method="post">
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>
            <input asp-for="ApiRole.Id" type="hidden" />
            <div class="mb-3">
                <label asp-for="ApiRole.Name" class="form-label"></label>
                <input asp-for="ApiRole.Name" class="form-control" />
                <span asp-validation-for="ApiRole.Name" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-success mb-1">Update</button>
        </form>
        <a asp-page="./Index">Back to List</a>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}